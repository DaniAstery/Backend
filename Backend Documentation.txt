Connection str for mongodb 

mongodb+srv://<db_username>:<db_password>@cluster0.aktm1.mongodb.net/


sudo docker stop mongodb

sudo docker start mongodb

sudo docker rm -f mongodb

connection string        



starting backend 
yarn start 


mnoTSO0FxEtA1j83

running public ip 
ngrok http 3000
https://arlette-unpawed-jermaine.ngrok-free.dev   

MONGO_URI=mongodb+srv://dani:dani@asteryacluster.zblomdw.mongodb.net/?AsteryaDB=AsteryaCluster





// 🔥 FORCE CORS + PREFLIGHT (MUST BE FIRST MIDDLEWARE)
app.use((req, res, next) => {
      res.header(
        "Access-Control-Allow-Origin",
        "https://daniasterygithubio-production.up.railway.app"
      );
      res.header(
        "Access-Control-Allow-Methods",
        "GET,POST,PUT,DELETE,OPTIONS"
      );
      res.header(
        "Access-Control-Allow-Headers",
        "Content-Type, Authorization"
      );

        if (req.method === "OPTIONS") {
          return res.sendStatus(204);
        }

  next();
});


mongodb password  inadinad